This is my dotfiles for MacOS managed with GNU stow.

## Installation

Install the necessary dependencies with homebrew:

```bash
brew bundle install
```

Then, stow the packages using the installation script:

```bash
sh install.sh
```
